using MusicManagementCore.Domain.Audio;

namespace MusicManagementCore.Util;

/// <summary>
/// Builds the output directory names of audio file meta data. 
/// </summary>
public class CompressedDirNameBuilder(string format)
{
    public string Build(MetaData metaData)
    {
        return "";
    }
}